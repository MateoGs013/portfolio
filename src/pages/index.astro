---
// Home — edición Afiche (la default, la 1ª impresión del master). El cuerpo
// vive en Portada.astro (compartido con /terminal, /plano, /fanzine). La
// edición la fija esta ruta vía la prop `edicion`.
import Base from '../layouts/Base.astro';
import Portada from '../components/Portada.astro';
---

<Base edicion="afiche">
  <Portada />
</Base>
