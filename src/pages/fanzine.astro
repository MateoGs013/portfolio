---
// Edición Fanzine (gen 3, secreta: la fotocopia de las fotocopias). La ruta
// existe siempre (es "secreta", no protegida); el enlace en el selector se
// revela sólo con el clicker (ms-fanzine). Mismo contenido que la home.
import Base from '../layouts/Base.astro';
import Portada from '../components/Portada.astro';
---

<Base edicion="fanzine">
  <Portada />
</Base>
