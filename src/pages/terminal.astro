---
// Edición Terminal (gen 2 del linaje: el master transmitido a pantalla).
// Mismo contenido que la home; la edición la fija esta ruta. La física propia
// de Terminal aún no existe (fase posterior — primero Afiche impecable).
import Base from '../layouts/Base.astro';
import Portada from '../components/Portada.astro';
---

<Base edicion="terminal">
  <Portada />
</Base>
