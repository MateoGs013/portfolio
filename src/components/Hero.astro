---
// Portada del pliego: primera ESCENA (DESIGN.md §4). El masthead ocupa el
// viewport completo y el nombre compone en diagonal a escala de plancha
// (Mateo arriba a la izquierda, Sonzogni abajo a la derecha, sangrando los
// bordes). Detrás, el retrato del impresor compuesto en tipos móviles
// (scripts/tipos.ts). El pie de la escena es el marco vivo del taller
// (reloj en scripts/vivo.ts). El efecto registro vive en
// scripts/registro.ts; métricas por edición en ediciones.css.
import { getImage } from 'astro:assets';
import retratoOriginal from '../assets/retrato.png';

const retrato = await getImage({ src: retratoOriginal, width: 520, format: 'webp' });
---

<section class="portada" aria-label="Portada">
  <header class="masthead">
    <canvas class="tipos" data-tipos data-src={retrato.src} aria-hidden="true"></canvas>
    <div class="guia" data-guia aria-hidden="true"></div>
    <div class="scanline only-terminal" data-scanline aria-hidden="true"></div>
    <div class="caret only-terminal" data-caret aria-hidden="true"><span class="caret-blink"></span></div>
    <div class="cruz only-plano" data-cruz aria-hidden="true">
      <span class="cruz-h" data-cruz-h></span>
      <span class="cruz-v" data-cruz-v></span>
    </div>
    <div class="cota only-plano" data-cota aria-hidden="true">
      <span class="cota-line"></span>
      <span class="cota-label"></span>
    </div>
    <div class="mancha only-fanzine" data-mancha aria-hidden="true"></div>
    <div class="wrap masthead-top">
      <p class="folio only-afiche">Año I · Pliego Nº 001 · Buenos Aires · sale cuando hay algo que mostrar</p>
      <p class="folio only-terminal">
        MATEO_SONZOGNI(1) · sección 001 · cursor <span data-shell-cursor>—</span>
      </p>
      <p class="folio only-plano">
        Lámina 001 · portada · esc 1:1 · cursor <span data-plano-cursor>—</span>
      </p>
      <p class="folio only-fanzine">fanzine m·s · edición pirata · nº 001 · fotocopiado en buenos aires</p>
    </div>

    <h1 class="hero-title">
      <span class="linea"><span class="registro" data-text="Mateo">Mateo</span></span>
      <span class="linea linea-b"><span class="registro" data-text="Sonzogni">Sonzogni</span></span>
    </h1>

    <div class="masthead-pie">
      <div class="masthead-rule" aria-hidden="true"></div>
      <div class="wrap vivo" data-reveal>
        <p class="vivo-dato"><span data-reloj>--:--:--</span> · Buenos Aires</p>
        <p class="vivo-dato vivo-instrumento only-afiche" aria-hidden="true">
          marca <span data-cursor>—</span> · presión
          <span class="presion" data-presion aria-label="presión"
            ><i></i><i></i><i></i
          ></span>
        </p>
        <p class="vivo-dato vivo-estado"><span class="pulso" aria-hidden="true"></span> taller abierto a encargos</p>
      </div>
    </div>
  </header>

  <div class="wrap">
    <div class="portada-grid">
      <h2 class="titular only-afiche" data-reveal>
        Dos clientes en producción en España, un SaaS propio y una tesis que respira WebGL.
      </h2>
      <h2 class="titular only-terminal" data-reveal>
        $ uptime → 2 clientes en producción · 1 SaaS propio · 1 tesis WebGL. Nada guardado en un
        cajón.
      </h2>
      <h2 class="titular only-plano" data-reveal>
        Memoria descriptiva: dos obras entregadas y en uso en España, un producto propio y una
        tesis en cálculo.
      </h2>
      <h2 class="titular only-fanzine" data-reveal>
        Dos clientes vivos, un SaaS y una tesis. Todo hecho a mano, nada de plantillas.
      </h2>
      <div class="portada-rail" data-reveal>
        <p class="bajada">
          Creative developer. Cada trabajo sale de esta imprenta con identidad propia — este
          sitio también: <strong>cambiá la edición arriba y mirá cómo se reimprime</strong>.
        </p>
        <a class="cta" href="#tirada">Ver la tirada ↓</a>
      </div>
    </div>
  </div>
</section>

<style>
  /* ---- Escena 1: el masthead ocupa el viewport ---- */
  .masthead {
    position: relative;
    min-height: calc(100svh - 52px); /* 52px ≈ topbar sticky */
    display: flex;
    flex-direction: column;
    overflow: clip; /* la plancha sangra los bordes sin generar scroll */
  }

  /* Retrato compuesto en tipos: centrado detrás del nombre, apoyado
     sobre la regla — el nombre pasa por delante de la figura */
  .tipos {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    bottom: 64px;
    height: min(96%, 92svh);
    aspect-ratio: 3 / 4;
    z-index: 0;
    pointer-events: none;
  }
  .masthead-top,
  .hero-title,
  .masthead-pie {
    position: relative;
    z-index: 1;
  }

  /* Guía de registro: hairline vertical que sigue al cursor por toda la
     escena (scripts/vivo.ts la mueve; no es un cursor custom — el puntero
     del sistema queda intacto, esto es la página midiéndose) */
  .guia {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    width: 1px;
    background: var(--accent);
    opacity: 0;
    z-index: 2;
    pointer-events: none;
    will-change: transform, opacity;
  }
  /* Scanline CRT: en Terminal reemplaza a la guía vertical. Una línea
     horizontal con glow que sigue al cursor en Y. */
  .scanline {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    height: 2px;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent);
    opacity: 0;
    z-index: 2;
    pointer-events: none;
    will-change: transform, opacity;
  }
  /* Caret mono block: el cursor de la terminal persiguiendo al mouse.
     El blink es CSS puro dentro; la posición es GSAP quickTo. */
  .caret {
    position: absolute;
    top: 0;
    left: 0;
    width: 0.55em;
    height: 1em;
    font-size: 12px;
    opacity: 0;
    z-index: 3;
    pointer-events: none;
    will-change: transform, opacity;
  }
  .caret-blink {
    display: block;
    width: 100%;
    height: 100%;
    background: var(--accent);
    animation: caret-blink 1.06s step-end infinite;
  }
  @keyframes caret-blink {
    0%, 55% { opacity: 1; }
    56%, 100% { opacity: 0; }
  }
  /* Cruz filar (Plano): dos hairlines que cruzan en el cursor, como una
     mira de CAD. Reemplaza a la guía vertical de Afiche. */
  .cruz {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 2;
  }
  .cruz-h,
  .cruz-v {
    position: absolute;
    background: var(--accent);
    opacity: 0;
    will-change: transform, opacity;
  }
  .cruz-h {
    left: 0;
    right: 0;
    top: 0;
    height: 1px;
  }
  .cruz-v {
    top: 0;
    bottom: 0;
    left: 0;
    width: 1px;
  }
  /* Cota arquitectónica (Plano): línea de dimensión con ticks en los
     extremos y label con la medida real del elemento. Aparece debajo
     del elemento acotado. */
  .cota {
    position: absolute;
    pointer-events: none;
    z-index: 2;
    opacity: 0;
    will-change: opacity;
  }
  .cota-line {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: var(--accent);
  }
  .cota-line::before,
  .cota-line::after {
    content: '';
    position: absolute;
    top: -3px;
    width: 1px;
    height: 7px;
    background: var(--accent);
  }
  .cota-line::before {
    left: 0;
  }
  .cota-line::after {
    right: 0;
  }
  .cota-label {
    position: absolute;
    top: 5px;
    left: 50%;
    transform: translateX(-50%);
    font-family: var(--font-m);
    font-size: 10px;
    letter-spacing: 0.04em;
    color: var(--accent);
    background: var(--bg);
    padding: 0 4px;
    white-space: nowrap;
  }
  /* Mancha de tinta (Fanzine): la fotocopia se mancha bajo el dedo.
     Multiply en claro (oscurece), screen en oscuro (aclara) — la tinta
     fresca reacciona al toner del fondo. */
  .mancha {
    position: absolute;
    width: 220px;
    height: 220px;
    border-radius: 50%;
    background: radial-gradient(circle, var(--accent) 0%, transparent 68%);
    mix-blend-mode: multiply;
    opacity: 0;
    pointer-events: none;
    z-index: 1;
    will-change: transform, opacity;
    transform: translate(-50%, -50%);
  }
  .masthead-top {
    width: 100%;
    padding-top: clamp(12px, 2.2svh, 24px);
  }
  .folio {
    margin: 0;
    font-size: 11px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--soft);
    overflow-wrap: anywhere;
  }

  .hero-title {
    /* Cada edición pisa --hero-fs en ediciones.css (anchos de fuente distintos) */
    --hero-fs: min(23vw, 30svh);
    margin: auto 0;
    display: flex;
    flex-direction: column;
    gap: 0.03em;
    font-family: var(--font-d);
    font-weight: var(--weight-d);
    font-size: var(--hero-fs);
    line-height: 0.88;
    color: var(--ink);
  }
  .linea {
    display: block;
    margin-left: -0.02em; /* sangrado óptico contra el borde */
  }
  .linea-b {
    align-self: flex-end;
    margin-left: 0;
    margin-right: -0.02em;
    text-align: right;
  }
  .registro {
    position: relative;
    display: inline-block;
    /* variables animadas por registro.ts */
    --r1x: 0em;
    --r1y: 0em;
    --r2x: 0em;
    --r2y: 0em;
    --rega: 0;
  }
  .registro::before,
  .registro::after {
    content: attr(data-text);
    position: absolute;
    inset: 0;
    z-index: -1;
    opacity: var(--rega);
    pointer-events: none;
  }
  .registro::before {
    color: var(--reg-c1);
    transform: translate(var(--r1x), var(--r1y));
  }
  .registro::after {
    color: var(--reg-c2);
    transform: translate(var(--r2x), var(--r2y));
  }

  /* ---- Pie de escena: regla full-bleed + marco vivo ---- */
  .masthead-rule {
    width: 100%;
  }
  .vivo {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
    width: 100%;
    padding-top: 10px;
    padding-bottom: 14px;
  }
  .vivo-dato {
    margin: 0;
    font-family: var(--font-m);
    font-size: 12px;
    letter-spacing: 0.06em;
    color: var(--soft);
    font-variant-numeric: tabular-nums;
  }
  .vivo-estado {
    color: var(--ink);
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .pulso {
    width: 9px;
    height: 9px;
    background: var(--accent);
    display: inline-block;
    animation: pulso 2.2s infinite;
  }
  @keyframes pulso {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.25;
    }
  }

  /* ---- Escena 2: titular de portada, con aire propio ---- */
  .portada-grid {
    min-height: min(80svh, 760px);
    align-content: center;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: clamp(18px, 3vw, 40px);
    padding: clamp(40px, 8svh, 90px) 0;
    align-items: start;
  }
  .titular {
    grid-column: 1 / 8;
    margin: 0;
    font-family: var(--font-d);
    font-weight: var(--weight-d);
    font-size: clamp(1.7rem, 0.9rem + 3.4vw, 4.2rem);
    line-height: 1.08;
    text-wrap: balance;
  }
  .portada-rail {
    grid-column: 9 / 13;
    display: flex;
    flex-direction: column;
    gap: 22px;
    border-left: 1px solid var(--line);
    padding-left: clamp(14px, 1.6vw, 24px);
  }
  .bajada {
    margin: 0;
    color: var(--soft);
    font-size: 0.98rem;
    line-height: 1.6;
    max-width: 34ch;
  }
  .bajada strong {
    color: var(--ink);
    font-weight: 600;
  }
  .cta {
    align-self: flex-start;
    font-family: var(--font-m);
    font-size: 13px;
    letter-spacing: 0.06em;
    text-decoration: none;
    color: var(--ink);
    border-bottom: 3px solid var(--accent);
    padding-bottom: 3px;
    transition: color 0.15s;
  }
  .cta:hover {
    color: var(--accent);
    /* misregistration de hover: los canales se corren un pelo */
    text-shadow:
      -2px 0 var(--reg-c1),
      2px 0 var(--reg-c2);
  }

  @media (max-width: 860px) {
    .titular {
      grid-column: 1 / 13;
    }
    .portada-rail {
      grid-column: 1 / 13;
      border-left: none;
      padding-left: 0;
      border-top: 1px solid var(--line);
      padding-top: 18px;
    }
  }
  @media (max-width: 640px) {
    .masthead {
      /* Topbar envuelta en dos filas; tope para que en pantallas muy altas
         la escena no quede vacía y la siguiente asome (invita a scrollear) */
      min-height: min(calc(100svh - 128px), 660px);
    }
    .vivo-dato {
      font-size: 11px;
    }
    .tipos {
      bottom: 78px;
      height: 76%;
    }
  }
</style>
